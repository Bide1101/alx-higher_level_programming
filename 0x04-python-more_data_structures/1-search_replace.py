#!/usr/bin/python3
def search_replace(my_list, search, replace):
    new_list = []

    for i in my_list:
        if i == search:
            new_listppend(replace)
        else:
            new_list.append(element)

    return new_list
